print_stego(image_name):
	load an image
	look at each pixel
	get the pixel's blue value
	convert the value to binary
	record last digit of binary into list
	combine list 8 items at a time into new list of binary values
	print char of each binary value

create_stego(image_name):
